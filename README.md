# llm

A MATLab simulation generator using OpenAI's GPT 4.0 to create YAML test files based on user-inputted prompts. 

Steps:

1. Set up conda environment from 'environment.yml'
2. Add OpenAI API key to '.env'
3. Run Program! 
